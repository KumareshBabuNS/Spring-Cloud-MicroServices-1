spring:
  application:
    name: API-Gateway
  cloud:
    config:
      uri:
      profile: production
      #label: master
      discovery:
              enabled: true
              service-id: Configuration-Center
server:
  port: 7000
zuul:
  routes:
    search:
      path: /search/**
      serviceId: Search-Service
    suggest:
      path: /suggest/**
      serviceId: Suggest-Service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
