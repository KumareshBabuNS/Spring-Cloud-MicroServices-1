spring:
  application:
    name: API-Gateway
  cloud:
    config:
      uri:
      profile: production
      label: master
      discovery:
              enabled: true
              service-id: Configuration-Service
encrypt:
  failOnError: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/